spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:${mangaserver.database}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
  data:
    rest:
      base-path: /api

mangaserver:
  database: database.sqlite
